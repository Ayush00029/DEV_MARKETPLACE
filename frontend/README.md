# Dev Marketplace Frontend

React + Vite frontend for the Dev Marketplace API.

## Run locally

```bash
npm install
npm run dev
```

In development, Vite proxies `/api` requests to `http://localhost:5000`.
Set `VITE_API_URL` to point at another backend in deployed environments.
